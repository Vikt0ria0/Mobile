<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Mobile.Views"
    Shell.FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="false"
    Title="Mobile">

    <ShellContent
        Title="Цели на день"
        ContentTemplate="{DataTemplate views:DailyGoalsPage}"
        Route="dailygoals" />

    <ShellContent
        Title="Основные цели"
        ContentTemplate="{DataTemplate views:MainGoalsPage}"
        Route="maingoals" />

    <ShellContent
        Title="Календарь"
        ContentTemplate="{DataTemplate views:CalendarPage}"
        Route="calendar" />

</Shell>
